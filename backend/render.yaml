# Not a Render Blueprint file. Use `render.yaml` at the repository root.
#
# If your Render service "Root Directory" is `backend`, set in the dashboard:
#   Build:  npm install --no-audit --no-fund && npm run build
#   Start:  node dist/server.js
#
_placeholder: null
